{
  "name": "@harness/service",
  "version": "0.0.0",
  "private": true,
  "description": "The deployable service — Railway boot target for the AF-004 provisioning run (minimal boot skeleton; product surface lands per its own issues).",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.6.0"
  }
}
