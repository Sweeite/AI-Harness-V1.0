# AI Harness — Requirements Specification

This repo turns the **design doc** (`spec/source/design-doc-v4.md`) into a **build-ready
requirements spec**: atomic, testable, fully traceable, zero ambiguity.

Point A = a narrative design doc. Point B = a spec that decomposes into GitHub issues,
where every built artifact traces back to a requirement you signed off on.

---

## How this repo is organised

```
spec/
  source/              The original design doc + extraction scaffolding (read-only reference)
  00-foundations/      Decide-once layer: conventions, glossary, ADRs, standards, decision log
    process-overview.md  Full optics — what/want/goal/why/how (read after CLAUDE.md)
    phase-playbooks.md   The repeatable procedure for every phase (0→6)
    system-map.md        Top-down view: how it all fits + the e2e request route
    system-map/          Per-component zoom-ins + failure-overlay (the shadow map)
    what-makes-it-great.md  The quality bar + honest coverage audit (great vs good)
    working-with-me.md   Grounding mode — how to support the user when overwhelmed
    glossary.md          Every load-bearing term, defined once
    id-conventions.md    The ID scheme (FR/CFG/UI/DATA/PERM/AC/OD/ADR)
    requirement-template.md  The shape every functional requirement takes
    open-decisions.md    The OD log — every unresolved question (the anti-ambiguity gate)
    out-of-scope.md      Things consciously NOT built / deferred to v2 (OOS-*)
    feasibility-register.md  Assumptions that can only be proven by testing (AF-*)
    adr/                 Architecture Decision Records (the 7 load-bearing decisions + more)
    standards/           Cross-cutting patterns (config edit taxonomy, change control, migration discipline, tool-integration research, RBAC, UI states)
    tool-integrations/   Per-tool research dossiers (research-first gate for every new connector) + template + index
  01-requirements/     Functional requirements, one file per component (0..10)
  02-config/           Config registry (every tunable, classified + surfaced)
  03-surfaces/         Dashboard/UI specs (every surface, all states)
  04-data-model/       Consolidated schema, RLS, indexes, migrations
traceability-matrix.csv  The master index — walk any requirement end to end
```

## The traceability spine ("ribbons")

```
design-doc line ─► FR-<comp>.<area>.<n> ─► {CFG-*, UI-*, DATA-*, PERM-*} ─► AC-* ─► ISSUE# ─► PR# ─► TEST
```

Every ID is stable. The traceability matrix lets you walk from any design intent to the
code that implements it and the test that proves it — in both directions.

## The anti-ambiguity rule

A requirement cannot reach status `Ready` while any **Open Decision (OD-*)** points at it.
Nothing is "assumed." Everything ambiguous becomes a tracked OD with options + a
recommendation, and stays open until you resolve it.

---

## The plan (phases, dependency-ordered)

| Phase | What | Done when |
|---|---|---|
| 0 | Foundations: glossary, 7 ADRs, standards, templates, matrix | Conventions locked; load-bearing ADRs resolved |
| 1 | Functional requirements per component (0→10) | Every design line maps to ≥1 FR; zero open ODs per FR |
| 2 | Config registry: classify + surface every key | Every CFG has surface + edit-mechanism + validation; zero `???` |
| 3 | Dashboard/UI specs: every surface, all states | Every UI- surface fully specified |
| 4 | Data model: schema, RLS, indexes, migrations | Every DATA- ref consolidated + consistent |
| 5 | Non-functional: security, infra, observability, cost, compliance, **backup & disaster recovery**, test strategy | All NFRs explicit |
| 6 | Issue decomposition: vertical slices → GitHub issues **+ build-order / dependency map** | Every FR maps to an issue; every issue back to FRs; build sequence defined |

**Standing verification gate:** after each component, an independent agent re-extracts FRs
from the design prose and confirms no design line is orphaned. Mechanizes "nothing left out."

**Parallel feasibility track:** a spec proves the design is *coherent*, not that it *works*.
Assumptions that can only be confirmed by testing are logged in
`spec/00-foundations/feasibility-register.md` (IDs `AF-*`) with a verification method
(DOCS / SPIKE / EVAL / LOAD), and tagged `⚠️ FEASIBILITY` wherever they're relied on. Four
priority spikes (cost, memory retrieval, vendor-claims, provisioning) run alongside the ADR
phase because they can invalidate the architecture. Paper-vs-proven is always stated openly.

## Working rhythm (per component)

1. **I draft** the FR set + the OD list (decisions needed from you), each OD with a recommendation.
2. **You decide** the ODs.
3. **I finalize** acceptance criteria, wire traceability, run the verification agent, commit.

## Definition of done for the spec

- Every design-doc line traces to ≥1 FR (no orphans).
- Every FR is atomic, has acceptance criteria, has zero open decisions.
- Every config: captured, classified, surfaced, edit-mechanism defined, validated.
- Every surface: fully specified with all states.
- Every component explicitly **signed off by you** before it proceeds to build.
- Every exclusion / deferral logged in `out-of-scope.md` (no silent scope drift).
- Every FR → a GitHub issue; every issue → back to FRs, in a defined build order.

---

## Status

| Phase | Status |
|---|---|
| 0 — Foundations | 🟢 complete |
| 1 — Requirements | 🟢 **COMPLETE — all 11 components C0–C10 Approved (2026-06-27)** · **C0 (Login) ✅ Approved** (42 FRs) · **C1 (RBAC) ✅ Approved 2026-06-24** (37 FRs) · **C2 (Memory) ✅ Approved 2026-06-25** (57 FRs — 56 Approved + 1 v2-deferred; OD-032…OD-038 resolved; verification gate clean — orphan/contradiction CLEAN + 7 quality findings reconciled; AF-082, OOS-016/017 logged; matrix + system-map wired). **C4 (Prompt Architecture) ✅ Approved 2026-06-26** (session 21) — **32 FRs** (LYR ×4 · CID ×6 · BIZ ×3 · INJ ×4 · TSK ×3 · PRIN ×3 · STO ×6 · OPT ×3), a content-definition component (C4 owns the layer content + storage; runtime assembly → C5, enforcement → C6). OD-048…OD-053 resolved; verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS) + 7 quality findings reconciled (+FR-4.LYR.004 assembly-validates-safety-elements, principles-edit audit re-anchored, +the OD-053 principles **hard-floor**). AF-111 logged (EVAL, gates only the optimisation claim); matrix (32 rows) + system-map wired. **C5 (Agent Harness) ✅ Approved 2026-06-26** (session 22) — **43 FRs** (TRG ×5 · QUE ×6 · GRP ×4 · ENV ×3 · LOP ×5 · JOB ×7 · ASM ×9 · OPT ×4), the execution layer (triggers · task_queue · task graphs · context envelope · 3 loops · Inngest + DLQ · prompt-stack assembly + run pipeline · optimisations); enforcement → C6, observability → C7, orchestration → C8 are seams. OD-054…OD-059 resolved (OD-056 step-level approval + no-irreversible-outrun and OD-059 fresh-envelope chaining are the two #2-touching, user-decided; OD-054/055/057/058 delegated). Verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS) + 11 quality findings reconciled (+FR-5.TRG.005 at-least-once event→task seam-atomicity, approval-wait + DLQ + chain-fire escalation reusing the C1/C2 don't-silently-abandon pattern, crash-window idempotency ordering, quarantine retains WIP). AF-112…AF-115 logged (block P — catch-up idempotency, parallel-DAG, compression fidelity, originals-store retention; all build-time, none hold an FR). Matrix (43 rows) + system-map wired. **C6 (Guardrails) ✅ Approved 2026-06-26** (session 23) — **36 FRs** (HRD ×4 · APR ×6 · ANM ×5 · RTL ×4 · ESC ×4 · INJ ×6 · LOG ×4 · OPT ×2 · FMM ×1 — RTL ×4 includes FR-6.RTL.004, cost-ladder enforcement, added via change control 2026-06-27 to close the OD-068 debt owed from C7), the enforcement layer ("the code half" of safety): code-side hard-limit enforcement · the 3 approval tiers + mandatory-hard set + contextual routing · 5 pre-step anomaly checks · rate-limit guardrails · the escalation/flagged workflow · the 4-step injection sanitization pipeline · the `guardrail_log` · optimisations. **ADR-007 is the spine** (containment-first; semantic scan off-by-default; thresholds are signal knobs; quarantine retains-not-discards). Carry-forwards **OD-047** (keep the seven hard limits absolute + gate-don't-promote coverage gaps) and **OD-010** (no auto-rollback + human-visible cleanup task) resolved here; OD-060…OD-066 resolved (the four #2-touching — OD-060 hard-limit-not-overridable, OD-064 soft-approval reversible-only, OD-047, OD-010 — surfaced to the operator, who delegated). Verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS, the failure-map kept seamed not absorbed) + **12 quality findings reconciled** (the 3 HIGH were mechanism-wiring holes: +AC-6.INJ.001.2 named-harness-call-site, +AC-6.FMM.001.3 guardrail-check-itself-errors-fails-closed, +AC-6.LOG.003.3 log-write-failure-is-fail-closed; +M1 no-self-approval-at-human-tier, +M2 multi-fire-precedence, +M3 manifest tightened, +M4/M5 wait-point staleness owners, +M6/L1/L2/L3). AF-116/117 logged (EVAL — anomaly accuracy + injection-library coverage; build-time, none holds an FR); AF-068 still gates the enforceability *claim* of the hard limits. Matrix (35 rows) + system-map wired. **C7 (Observability) ✅ Approved 2026-06-26** (session 24) — **33 FRs** (LOG ×7 · RTP ×4 · ALR ×8 · COST ×4 · MGM ×5 · VIEW ×3 · OPT ×2), the **observability backbone** — `event_log` · the real-time-vs-polling contract · alerting (7 rules + routing + escalation + the engine watchdog) · the cost meter + ladder signal · the management-plane cross-deployment push (ADR-001 §7) + backup-health (ADR-008) · log retention/export. **Scope call (operator): backbone now, the 5 dashboard *surfaces* → Phase 3**; each panel's *signal* is produced by its home component (C2/C3/C5/C6/C8/C9). OD-067…OD-074 resolved (OD-068 cost-ladder ownership — C7 meters/signals, C6 decides, C5 executes, grounded in ADR-003 — and OD-074 log-erasure redaction-tombstone are the two user-decided #2/#1 calls; rest delegated). Verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS) + **13 quality findings reconciled** (the strongest #3 backbone yet; the residual risk was the observability layer becoming its OWN silent point of failure: +FR-7.ALR.008 alert-engine watchdog, +AC-7.MGM.002.3 independent-heartbeat stale-detector, +AC-7.LOG.003.2 out-of-band log-failure path, +AC-7.LOG.003.3 cross-sink reconciliation, server-authoritative time, cost-unknown sentinel; F1 cost-seam corrected against ADR-003 — C5's "C7 enforces" line fixed via change-control + the owed C6 cost-ladder FR tracked). AF-118…AF-120 logged (block R — absence-of-signal liveness, out-of-band durability, clock-sync; all build-time, none holds an FR). OOS-028 (self-hosted Inngest, owed from C5) + OOS-029 (cross-deployment benchmarking, v2) logged. Carry-forward: C2 FR-2.MNT.017 owes a log-sink erasure amendment. Matrix (33 rows) + system-map wired. **C8 (Agent Design) ✅ Approved 2026-06-26** (session 25) — **37 FRs** (ORC ×8 · REG ×6 · SPC ×6 · SCO ×3 · PLAN ×4 · HLTH ×4 · LRN ×3 · COST ×3), the **routing + agent-definition layer**: the orchestrator + 7-step description-driven routing · the `agents` registry (data-driven, versioned; `system_prompt` removed → `prompt_layers`, closing OD-048; `client_slug` dropped intra-silo) · the 8 specialist definitions + their hard limits (Comms never-sends, Finance never-transacts, Memory = sole writer identity per ADR-004) · per-agent memory scoping · per-step failure-mode *assignment* (C5 executes) · agent-health / drift / dead-agent *metric production* (flag-never-auto-correct) · orchestrator learning + result caching · cost-routing by complexity. Execution/healing/dashboards/insight-generation seamed to C5/C2/C3/C7/C9. OD-075…OD-081 resolved (OD-076 #1 scope-aware cache invalidation · OD-077 #3 escalating clarification · OD-080 #2 Super-Admin-gated capability edits · **OD-081 #2 gate-raised** — the per-agent `memory_scope` had no enforcement consumer; **resolved + applied via change-control** to C5 AC-5.ASM.006.2 fail-closed + C2 AC-2.RET.004.2). Verification gate clean — orphan/contradiction CLEAN (caught + dropped the design's `agents.client_slug` vs ADR-001 §3, mirroring C7 OD-067) + **10 quality findings reconciled** (the 3 HIGH: H1 scope-wiring, H2 orchestrator crash-mid-route idempotent re-route, H3 metric-producer liveness/heartbeat). AF-121…126 logged (block S — routing accuracy · confidence calibration · drift/dead-agent detection · cache staleness · learning-improves-routing; all build-time EVAL/SPIKE, none holds an FR). OOS-030 (auto plan-rollback deferred). Carry-forward: the **C6 cost-ladder enforcement FR still owed** (OD-068). Matrix (37 rows) + system-map wired. **C9 (Proactive Intelligence) ✅ Approved 2026-06-27** (session 26) — **31 FRs** (MODE ×4 · PRO ×7 · SUG ×5 · CST ×7 · CMD ×8 — was 28 at sign-off; +FR-9.CMD.006–008 custom-commands addendum, surface-04/10), the **proactive-generation + cold-start-gating + chat-command layer**: the 3 proactivity modes (Suggest/Prepare/Act, mode = f(C6 risk tier), no-bypass) · the 7 generators (relationship/meeting/doc/derisking/opportunity/daily-briefing/pattern; each enable-disable-able + thresholded) · the suggestion lifecycle (`proactive_suggestions` store, rank/explain/deliver-route/dismissal-learn-with-floor) · the cold-start phase ladder (consumes C2's phase, owns proactive-suppression; enforcement seamed C6/C3/C5/C2/Phase-3) · the `/` command dispatch (node-gated, not the design's non-existent "Agency Owner" role). Enforcement→C6, slow-loop+trigger→C5, Insight-Agent-def→C8, coverage/[Building]→C2, delivery→C7, rendering→Phase 3 are seams. OD-082…087 delegated; **OD-088 (operator-decided #2)** = the configurable **action-autonomy matrix** narrowing C6's blanket "all external comms = hard" — low-risk external (cold-lead/non-client nurture) configurable to Prepare/Act-after-trust, **floored** (client/SoR comms, financial, Confidential/Restricted) fixed at hard — **applied via change-control to C6 FR-6.APR.002/003** + new FR-9.MODE.004. Verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS, caught + dissolved the "Agency Owner" role vs C1's six, mirroring C7/C8 `client_slug`) + **critical floor-narrowing check NO HOLE** (nothing financial/client/Restricted reaches Act — defended in depth incl. +AC-9.MODE.004.5 send-time re-resolution) + **9 quality findings reconciled** (H1 confident-but-wrong tag → AF-131; H2 Insight-risk floor spans dismissal+suppression+disable; M1 deferred-floor-item no silent expiry; M2 stuck-suggestion escalate; M3 node-gate-before-confirm; M4 floored-caps-mode; L1-L3). AF-127…131 logged (block T — signal-detection / dismissal-floor / ranking / ETA / tag-accuracy; all build-time, none holds an FR). OOS-031/032 (founder checklist + init guide = operational docs). Matrix (28 rows) + system-map wired. **C10 (Infrastructure & Compliance) ✅ Approved 2026-06-27** (session 27) — **34 FRs** (RET ×2 · DEL ×7 · OFF ×6 · PRV ×4 · MGT ×4 · DEP ×5 · MIG ×2 · ISO ×3 · LEG ×1), the **FINAL Phase-1 component** — the deployment / management-plane / lawful-deletion layer. Owns: the **intentional-retention** principle + retention configs · the **individual right-to-erasure** workflow (wraps C2 FR-2.MNT.017) · the **client offboarding** workflow (trigger → verified export + sign-off → retention-freeze → hard-delete/deprovision → compliance meta-record) · **provisioning** orchestration (ADR-005 §5) · the **release model** (auto-deploy · canary/release-train promotion gate · rollback-by-redeploy · version-skew alert · plugins-out-of-train) · **schema-migration propagation** + per-deployment failure isolation · the **management plane** (`client_registry` + ingest endpoint, ADR-001 §7) · **isolation** (`client_slug` deleted from app tables) + **residency**. **Scope call (operator-confirmed): functional infra + compliance in C10; backup/DR → Phase 5.** OD-089…OD-096 resolved (the 5 #1/#2/#3-touching delegated; **OD-096** carried the `client_slug` label-vs-delete tension to the ADR-001 §3 terminus). Verification gate clean — orphan/contradiction CLEAN (all 6 traps PASS, 10/10 citations) + **9 quality findings reconciled** (the 2 HIGH: **H1** frozen-vs-dark deployment seam → +AC-10.OFF.004.4 status-server-authoritative-not-dead-alert; **H2** export-verification-fails-closed → +AC-10.OFF.002.4; M1 erasure-verify-before-done → +AC-10.DEL.003.4 + C2 AC-2.MNT.017.5 + AF-137; M2-M4 fail-closed gates / resumable store / token-revoke-first; L1 enforcement consumer). AF-132…137 logged (block U — deprovision completeness · export integrity · erasure recall · freeze propagation · legal minimums · erasure-completeness verify; all build-time, none holds an FR). OOS-033 (automated plugin distribution). **Two Phase-1 debts cleared (change-control): OD-068** → new **C6 FR-6.RTL.004** (cost-ladder enforcement) · **OD-074** → **C2 AC-2.MNT.017.4** (log redaction-tombstone). **C5 AC-5.TRG.001.3** (OD-091 freeze gate) added. Matrix (34 rows) + system-map wired. · **C3 (Tool layer) ✅ Approved 2026-06-25** (session 20) — **53 FRs** (40 generic runtime: CONN/REG/TOK/RL/ACT-limits/TRIG/OPT/DSC + 13 connector instances: GHL/Google/Slack OBS/ACT/TOK/TRIG), citing the dossiers (not the design doc) for all vendor facts. Verification gate run: orphan/contradiction CLEAN (all 6 traps PASS) + **caught a real cross-component bug** — C0 FR-0.WHK.002 GHL webhook HMAC→**Ed25519**, corrected via change-control (**OD-046**); 10 quality findings reconciled (+FR-3.TRIG.005 watch re-arm, +FR-3.TRIG.006 event-gap reconcile, +8 AC tightenings). 3 viability gates hold FRs from build: Slack ingest (AF-083/084), GHL webhook (AF-090), GHL PHI (AF-098). Matrix (53 rows) + system-map wired. |
| 2 — Config | 🟢 **COMPLETE — signed off 2026-06-27 (session 28).** Registry + verification-gate CLEAN; defaults confirmed (operator: "as long as i can edit these later i am happy" — all knobs LIVE/BOOT, post-deploy editable). — `spec/02-config/config-registry.md`: ~117 knobs + 11 secrets + 10 structured objects, all classified/defaulted/validated/PERM-gated/surfaced; zero `???`. Gap-hunt added 8 platform SECRETs (incl. mgmt-plane token, Anthropic/OpenAI keys) + `ef_search` + alert-routing keys. **OD-097** (alert routing has no owner) resolved + **CLOSED via change-control C7 `FR-7.ALR.009`** (unroutable alert fails loud; C7 now 34 FRs). 3 conflicts + 2 class-calls resolved (locked-spec-beats-stale-design). Awaiting operator confirm of proposed defaults; then sign-off. |
| 3 — Surfaces | 🟢 **COMPLETE — all 14 surfaces signed off (2026-07-01, session 43).** `surface-00-auth.md` ✅ (2026-06-29 · 6 sub-surfaces UI-LOGIN/2FA-ENROLL/2FA-CHALLENGE/INVITE-SETUP/REAUTH-PROMPT/SUPPORT-REQUESTS · all 5 states each · verification gate CLEAN — 0 HIGH, 1 MED + 1 LOW patched (a3 aal1→2FA nav row, e1 FR-0.REC.006 notify-failure note) · OD-105–108 resolved · OOS-039 logged). `surface-01-config-admin.md` ✅ (613 lines · all 11 CFG sections · verification gate CLEAN · OD-098–103 resolved). `surface-02-user-mgmt.md` ✅ (2026-06-30 · 6 C1 admin sub-surfaces UI-USER-MGMT/ROLE-MGMT/PERMISSION-MATRIX/CLEARANCE-MGMT/CLEARANCE-REVIEW/RESTRICTED-GRANT, tabbed; renders C1 ROLE/PERM/CLR/RST/USR/AUD + C0 INV.001–007 invite lifecycle · all 5 states each · verification gate CLEAN — 0 HIGH, 1 MED + 2 LOW patched (CFG cadence-key edit-location → surface-01 `#guardrails` not `#auth`; UI-USER-ACTIVITY merge note; RLS.007 seam → C5/C6/C8), 2 LOW justified · OD-109–112 resolved · Admin gated to Users tab, role/perm/clearance/Restricted = Super-Admin-only). `surface-03-ingestion-queue.md` ✅ (2026-06-30 · the **"Memory Review"** surface — 3 human-gated queues guarding the memory write path, tabbed: **Ingestion** (UI-INGESTION-QUEUE, FR-2.ING.002/003/004/005 + trust-window audit FR-2.ING.001) · **Conflicts** (hard-conflict quarantine FR-2.WRT.002/OD-032) · **Consolidation** (Personal-tier merge/summarise approval FR-2.MNT.014/OD-037) · all 5 states each · verification gate CLEAN — 0 HIGH, 5 MED + 1 LOW reconciled · OD-113–116 resolved. **OD-115 minted two C1 Memory-Access PERM nodes via change-control** — `PERM-memory.review_conflict` + `PERM-memory.approve_consolidation` — closing a real Rule-0 gap (C2 FRs named no node for conflict/consolidation review). ADR-004 carry-in: Include/approval routes through the **sole writer**, never a direct insert). **Config registry carries a plain-English `What it does` description on every one of the 170 knobs/secrets + 11 structured objects** (canonical home; surfaces bind to it, DRY — `_TEMPLATE.md` carries the DRY rule). Phase 3 template + playbook at full detail. Surface list: 14 files (surface-00 auth → surface-12 mobile + surface-01b config-audit-log). `surface-04-approval-queue.md` ✅ (2026-06-30 · the **agent-action approval queue** — the realtime/WebSocket counterpart to surface-03's poll queues; **UI-APPROVAL-QUEUE** minted here, one of exactly two Realtime surfaces per FR-7.RTP.001). One live queue of held agent *actions* — `awaiting_approval` (C6 tier gates FR-6.APR.*) + `flagged` (safety holds FR-6.ESC.001) — with **Approve / Reject / Modify** (FR-6.ESC.003), a live auto-run countdown on reversible soft items (FR-6.APR.003), already-applied-side-effects + compensation-not-rollback (OD-010), and an honest Live/Polling/Reconnecting indicator (FR-7.RTP.004) whose offline state **re-fetches before re-enabling actions** (a soft item may auto-run server-side while offline). All 5 states each · verification gate CLEAN-WITH-FIXES — 1 HIGH + 3 MED reconciled (HIGH: stale `client_slug` "fate-undecided" → corrected to OD-096-deleted/C10 FR-10.ISO.001; +`originating_user_id`/`escalated_at` flagged as new Phase-4 fields; +soft-countdown server-authoritative re-cite; +PERM node re-homed) · OD-117–120 resolved. **OD-117 minted `PERM-action.review` via change-control** under the existing **Approval Authority** category (FR-1.PERM.007) — closing a real Rule-0 gap (C5/C6/C7 named no node for *deciding* a held item). **OD-120 amended C6 FR-6.APR.003 (AC-6.APR.003.3)** — a reviewer-side "Hold for full review" promotes a soft item to explicit-approval, stopping the auto-run. C9 addendum: +FR-9.CMD.006–008 (custom Commands). OOS-034–038 (Objectives/Projects/Priority Matrix/Brain Dump/Field Ops → v2). `surface-05-dashboard-ops.md` ✅ (2026-06-30 · the **poll-based ops dashboard** — the read-only counterpart to surface-04's Realtime queue; **UI-DASHBOARD-OPS** minted here. Nine polled panels per FR-7.VIEW.001, each fed by its home component, each on its own cadence (FR-7.RTP.002): **System Health** (C5 loops/queue + C3/C8 rollups) · **Failure Health** (the silent-failure detector — `task_queue`-terminal-without-`event_log`-terminal, AC-7.LOG.003.1) · **Connector Health** (C3 DSC/TOK/RL/TRIG) · **Memory Health** (C2 MNT, read-only; actions→surface-03) · **Event Log** (C7 LOG.001–006) · **DLQ** (C5 JOB.006, human-only requeue/discard) · **Cost** (C7 COST — renders the meter + lit ladder rung; C6 decides/C5 executes, no enforcement claim) · **Guardrail Log** (C6 LOG + C7 LOG.007 export) · **Self-Improvement** (C8 HLTH/LRN + C9 suggestions, flag/suggest-only). Framed as the embodiment of **#3 (never fail silently)** — every panel's error/stale state refuses a false-healthy view (badge "—", not "0"/"$0"/"✓"); the silent-failure detector protects itself. All 5 states each · verification gate **CLEAN — 0 HIGH/0 MED/0 LOW** · OD-121–124 resolved. **OD-123 minted `dlq_stale_alert_hours` (24 h, LIVE, `#loops`) via change-control to the config registry** — closing a Rule-0 gap (C5 AC-5.JOB.006.2 assumed a DLQ-staleness knob the registry lacked). OD-121 (per-panel role-scoping) binds to **existing** C1 categories — **no node mint** (unlike surface-03/04). Single-deployment scope (OD-124 — fleet view is surface-06). `surface-06-dashboard-super-admin.md` ✅ (2026-06-30 · the **Super Admin management-plane / fleet console** — the **only cross-deployment surface in the product**, running on the **separate management deployment** (ADR-001 §7), not any client silo; **UI-DASHBOARD-SUPER-ADMIN** minted here. A grid of deployment cards fed by the **push-only, operational-metadata-only** ingest (FR-7.MGM.001–005 / FR-10.MGT.001–004) + the cross-fleet operator workflows: **Fleet Health Grid** · **Cross-Deployment Alerts** (incl. the self-watching alert-engine + unroutable-alert banners, FR-7.ALR.008/009) · **Releases & CI/CD** (promote/rollback + skew alert, FR-10.DEP.*) · **Migrations** (per-deployment failure isolation, FR-10.MIG.*) · **Provisioning & Onboarding** (FR-10.PRV.*) · **Cross-Deployment Cost** (estimate-grade, ADR-003) · **Backup Health** (Supabase Management API, FR-7.MGM.005) · **Client Registry & Offboarding** (the guarded 5-step destructive workflow, FR-10.OFF.* + two-person hard-delete FR-10.DEL.003/AC-10.DEL.006). Governed by **#2** — the management plane is *a map, not a warehouse* (FR-10.MGT.003: only operational metadata crosses, **no client business data ever**; to look inside a client the operator clicks through under *that client's* RBAC) — and **#3** (a dark deployment never reads healthy; **frozen ≠ dead** AC-10.OFF.004.4; staleness on an independent heartbeat + server-authoritative time AC-7.MGM.002.3/.4; offboarding never completes on a partial-silent deprovision AC-10.OFF.005.2; export verified-complete before deletion AC-10.OFF.002.4). **This is the one surface where `client_slug` is valid** — it lives only in `client_registry` (ADR-001 §3/§7 / FR-10.MGT.001/OFF.006), deleted from every app table (OD-096 / FR-10.ISO.001). All 5 states each (every error/stale state refuses a false-healthy view; destructive actions disabled when state is unconfirmed) · verification gate **CLEAN-WITH-FIXES — 0 HIGH/0 MED/3 LOW reconciled** (AF-tag precision: AC-7.MGM.002.4→AF-120, Backup→AF-069/070, +parent FR-10.DEL.003) · OD-125–128 resolved. **OD-125 minted five `PERM-fleet.*` management-plane nodes via change-control** (`view/provision/promote_release/offboard/rotate_token`) + introduced a new **`management-plane` PERM scope** — closing a real Rule-0 gap (C7/C10 named the operator/Super-Admin holder of every fleet action in prose but bound no node); transcribed into `PERMISSION_NODES.md` (catalog 37→42; also flagged 3 owed earlier nodes OD-115×2/OD-117 never transcribed). `surface-07-dashboard-agency.md` ✅ (2026-07-01 · the **agency-owner / manager non-technical dashboard + notification centre** — the business-activity counterpart to surface-05's technical ops view; **two surface IDs minted here: `UI-DASHBOARD-AGENCY`** (FR-7.VIEW.002's "Manager (non-technical)" role view) **+ `UI-NOTIFICATION-CENTRE`** (the **second of exactly two Realtime surfaces** per FR-7.RTP.001 — the other is surface-04's approval queue). Four sections: **Notification Centre** (Realtime; all 7 alert rules FR-7.ALR.002 + the 2 self-protective banners alert-engine-stalled/unroutable FR-7.ALR.008/009 + Slack-independent durability FR-7.ALR.006 + honest Live/Reconnecting/Polling FR-7.RTP.004 with offline re-fetch) · **At-a-Glance** (non-technical management rollup, C7 invents no signal AC-7.VIEW.001.1) · **Activity Feed** (a **canonical home of the answer-mode pill** Cited/Inferred/Unknown C4 FR-4.CID.006 / AC-7.VIEW.002.2; an unresolved pill reads "mode unknown", never silently "Cited") · **Proactive Suggestions** (C9 delivery FR-9.SUG.004; every "act" routes through C6 FR-9.MODE.003, dismissal safety-floor FR-9.SUG.005/AC-9.PRO.004.2/.4). **The notification centre is cross-cutting chrome** (FR-7.ALR.001 "accessible from every view", home-specced here, node-free, clearance-scoped). All 5 states each (every error state refuses a false-healthy view — "—"/"mode unknown", never "0"/"all clear"/"Live"-when-dropped) · verification gate **CLEAN-WITH-FIXES — 1 HIGH/1 MED/2 LOW reconciled** (HIGH: no-bypass rule re-cited FR-9.PRO.005→**FR-9.MODE.003** at 6 sites; MED: node-name drift `view_ops`→**`PERM-dashboard.ops`** reconciled across surface-05/07; LOW: RTP.002 cadence cite + AC-9.PRO.004.2/.4) · OD-129–132 resolved. **OD-129 minted the `PERM-dashboard.*` Dashboard Access family via change-control** — `PERM-dashboard.overview` (this surface) + `PERM-dashboard.ops` (formalising surface-05's dangling `view_ops` working name) under the already-homed FR-1.PERM.007 category, intra-client (catalog 42→44); the notification centre is node-free. Role mapping: "Agency Owner"→Super Admin, "Manager"→Admin/Account Manager (no invented roles). `surface-08-dashboard-user.md` ✅ (2026-07-01 · the **Standard User dashboard** — the everyday user's home, **`UI-DASHBOARD-USER`** minted here. Renders the design-doc canonical (L3256–3262) three Standard-User panels — **Chat interface · My Queue · Activity feed** — plus the two FR-mandated carry-ins (cross-cutting **notification centre** home-specced on surface-07, FR-7.ALR.001; **proactive suggestions** routed to the user, FR-9.SUG.004). Five sections: **A — Notification Centre** (cross-cutting chrome, Realtime, clearance-scoped) · **B — Chat interface** (the heart: `/` command dispatch FR-9.CMD.001–008, inline AI output each carrying the **answer-mode pill** C4 FR-4.CID.006 / AC-7.VIEW.002.2 — surface-08 is the *other* canonical pill home; destructive-command confirm-after-node-gate FR-9.CMD.003.3; `event_log` fail-closed FR-9.CMD.004.3) · **C — My Queue** (C5 `task_queue` filtered to this user; decisions route to surface-04) · **D — Activity Feed** (C7 `event_log`, clearance+relevance scoped, pill on every AI output) · **E — Proactive Suggestions** (C9 FR-9.SUG.004/005 delivered to the user; act-through-C6 FR-9.MODE.003; dismissal safety-floor; cold-start "learning" suppression FR-9.CST.002). Only the notification centre is Realtime (FR-7.RTP.001 caps it at two surfaces); chat/queue/feed **poll**. All 5 states each (every error/stale refuses a false-healthy view — "—" not "0", empty thread never "no history", an unresolved pill reads "mode unknown" never "Cited") · verification gate **CLEAN-WITH-FIXES — 2 HIGH/2 MED/1 LOW reconciled** (H1 OD-133–136 transcribed to `open-decisions.md`; H2 `PERM-dashboard.workspace` transcribed to catalog; M1 `PERM-action.review` annotated OD-117-owed; M2/L1 AC-cite precision) · OD-133–136 resolved. **OD-133 mints `PERM-dashboard.workspace` via change-control** — the third Dashboard Access node (default: all six roles), anticipated by surface-07/OD-129, completing the family (catalog 44→45). **OD-135 is the key architectural call:** the spec has **no chat-message store** — resolved to **persist the thread** (a net-new Phase-4 `conversations`/`messages` store owed to C5/C9 — losing history is a #1 violation) and return async task results **via poll + notification nudge, not a third Realtime socket** (honouring FR-7.RTP.001). `surface-09-agent-builder.md` ✅ (2026-07-01 · the **agent-management console** — **`UI-AGENT-BUILDER`** minted here, FR source **C8 (Agent Design)**. Five sections in three buckets: **Agent Fleet** (the data-driven `agents` roster grid + per-agent health/drift/dead-agent **badges** — full self-improvement panel stays on surface-05, badges + link-out only) · **Agent Builder** (the per-agent definition editor — description / memory scope / tools / max-tokens / enabled — every change an **immutable version with a mandatory `change_reason`** FR-8.REG.004) · **Version History** (the immutable trail) · **Orchestration & Routing** (the orchestrator-as-registry-agent ORC.008 + the routing-config **read-only readout**; knobs edited on surface-01 #agents) · **Execution Plans** (versioned plans + **human-only rollback** PLAN.004/OOS-030). The act-on counterpart to surface-05's self-improvement flags — in C8 the fix for mis-routing is **data, never code** (AC-8.ORC.003.1). Encodes the cardinal-sin defenses: the **hard-limit invariants are rejected AT WRITE** — Comms never-sends (AC-8.SPC.003.3), Finance never-transacts (AC-8.SPC.004.3), only the Memory Agent holds memory-write (AC-8.SPC.005.2 / ADR-004 sole writer) — a code-level deny, not a mere audit (#2); drift/dead-agent **flag-never-auto-correct** (OD-078); a **stalled health producer reads "stale" not green** (AC-8.HLTH.004.2, #3); no `client_slug` column (AC-8.REG.001.3). All 5 states each · verification gate **CLEAN-WITH-FIXES — 1 HIGH (already-resolved) + 2 LOW reconciled** (F1: the gate read the catalog *before* the transcription edit landed — the three `PERM-agents.*` nodes **are** present, catalog 45→48; F3: the Model field corrected to a **read-only config-derived display** — FR-8.REG.001 defines no `agents.model` column, a per-agent override would be net-new Phase-4; F2: count-baseline non-issue) · OD-137–140 resolved. **OD-137 mints the `PERM-agents.*` Asset Management node family via change-control** — `PERM-agents.view` + `.edit_description` (Super Admin + Admin) + `.edit_capability` (**Super Admin only**) — under the **existing** FR-1.PERM.007 "Asset Management" category (the design-doc's "Create / edit agents" row, L509–615), **encoding the locked OD-080 two-tier split** (capability edits tighter than the design-doc's coarse SA+Admin — a #2 authority decision); transcribed into `PERMISSION_NODES.md` immediately (new Asset Management section; catalog 45→48). `surface-10-commands.md` ✅ (2026-07-01 · the **custom-command management** surface — **`UI-COMMANDS`**, named by FR-9.CMD.006 not minted here. FR source **C9 (Proactive Intelligence)**: the custom-command CRUD FR-9.CMD.006–008 framed inside the broader `/` dispatch contract FR-9.CMD.001–005. Three sections: **Custom Commands** (the `commands` list — slug / assigned agent / invocation node / active-state; a disabled-agent command reads **inactive/"unavailable"**, never a silent no-op AC-9.CMD.006.3) · **Command Builder** (the definition editor — slug **collision-checked against all system slugs** AC-9.CMD.006.2 never silently renamed; `$ARGUMENTS` prompt template; **assigned agent required**; invocation-node picker default-deny AC-9.CMD.002.3) · **System-Command Reference** (read-only reserved namespace grouped by home component — system commands are **code-registered, not data**; `/tune` values edit on surface-01). Commands are **invoked on surface-08** (chat, inline pill FR-9.CMD.008); this surface only *defines* them. **The clean PERM case — no node minted**: `PERM-commands.manage` + `PERM-system.tune` **already catalogued** (C9 section, `PERMISSION_NODES.md` L89–90), unlike surfaces 03/04/06/07/08/09. All 5 states each (every error/stale refuses a false-healthy view — an empty list is a real cold-start not a masked failure, a collision is loud, an unknown-agent command shows "status unknown" not false-active) · verification gate **CLEAN — 0 HIGH/1 MED/2 LOW reconciled** (MED-1 OD-141–144 transcribed to `open-decisions.md`; LOW-1 catalog line-cite `L86–90`→`L89–90`; **LOW-2 OD-142/143 pushed into C9 via change-control**) · OD-141–144 resolved. **OD-142 + OD-143 amend C9 via change-control** — **AC-9.CMD.006.4** (#2 least-privilege: a manager may only gate a custom command on a node they're authorized to assign — no audience-widening past their own authority over the wrapped capability) + **AC-9.CMD.008.4** (#2 containment: a definition can add a UI confirm but can never *lower* the assigned action's C6 tier; every invocation runs the same C6 pipeline as any agent run) — so the two #2 constraints live in the requirement layer, mirroring surface-04 OD-120→AC-6.APR.003.3. **Phase-4 debt:** the net-new `commands` store (user-defined only; system commands stay code-registered; no `client_slug`; `active` auto-flips on assigned-agent disable) owed to C9/C5. `surface-11-memory-nav.md` ✅ (2026-07-01 · the **memory navigation / entity browser** — **`UI-MEMORY-NAV`** minted here, FR source **C2 (Memory)**. The **read/browse** counterpart to surface-03's write-path review queues: surface-03 gates the write, surface-11 navigates **what is stored**. Four sections: **Entity Browser** (the entity-organised spine FR-2.ENT.001, type-filtered, Maturity % + `[Building]` marker per entity, duplicate-cluster flag for the fragmentation/AF-082 risk) · **Entity Detail** (one entity's memories grouped by type + filled/empty knowledge slots FR-2.MAT.001 + `external_refs` pointers FR-2.ENT.004) · **Memory Detail** (one row in full — content, provenance, confidence + lifecycle FR-2.MNT.001, visibility×sensitivity tags, the **drillable supersede/summary chain** FR-2.MNT.007 nothing overwritten) · **Memory Search** (the dual keyword+vector search FR-2.RET.002, **clearance filter runs BEFORE ranking** FR-2.RET.004 — never shown-then-hidden). The human window into the three memory non-negotiables: **#1** integrity made visible (drillable chains, fragmentation, audited erasure MNT.017) · **#2** clearance/visibility/Restricted enforced **before display**, Restricted never auto-shown (explicit+audited reveal only, RST.003), Internal Org walled (ENT.003), every edit routes through the **sole writer** (ADR-004, never a direct `UPDATE`) · **#3** embed-failed memories never stored so never a silent partial (WRT.007), low-confidence/contradicted memories shown *with state*, a failed load reads "—" never a false-empty brain. **The clean PERM case — no entry node minted**: memory **read** is clearance-scoped at the row (FR-2.RET.004/FR-1.RLS.003), not node-gated (OD-145); mutations reuse `PERM-memory.write` (writer-routed) / `.delete`. All 5 states each · verification gate **CLEAN — 0 HIGH/0 MED/2 LOW (both benign)** · OD-145–148 resolved. **Catalog housekeeping this session:** the **3 long-owed nodes** — `PERM-memory.review_conflict` + `PERM-memory.approve_consolidation` (OD-115) + `PERM-action.review` (OD-117) — **transcribed into `PERMISSION_NODES.md`** (48→51), closing a standing Rule-0 dangling-ID debt; one stale C2 cross-ref corrected (FR-2.ENT.005 MNT.011→MNT.010). **Phase-4 debt:** the clearance-scoped browse read-path (`DATA-memories`/`DATA-entities`, no `service_role` browse, no `client_slug`) + the sole-writer submit path for human corrections. `surface-12-mobile.md` ✅ (2026-07-01 · the **mobile view** — action on the go — the cross-component mobile treatment the prior twelve surfaces each seamed here. Grounded in the design-doc canonical **"Dashboard 5 — Mobile view"** (L3266–3284: *"Purpose-built for action on the go. Not a scaled down desktop… Deep system management stays on desktop."*); mobile granted to **all six roles** (L538). **Six sub-surfaces minted:** **`UI-MOBILE-HOME`** (the glance — health score FR-7.VIEW.002 / pending-approvals count / active-alerts count / quick-chat) · **`UI-MOBILE-APPROVALS`** (the primary action surface — one-tap Approve/Reject, **Modify degrades to desktop**; one of the two Realtime surfaces, FR-6.APR.*/ESC.*) · **`UI-MOBILE-ACTIVITY`** (plain-English feed, the **answer-mode pill** on every AI output FR-4.CID.006 / AC-7.VIEW.002.2) · **`UI-MOBILE-CHAT`** (`/` dispatch, each command node-gated FR-9.CMD.002, `event_log` fail-closed FR-9.CMD.004.3) · **`UI-MOBILE-COMMAND-MENU`** (the tap-optimised quick-tap buttons above the keyboard, FR-9.CMD.005/L3915 — same node gate + C6 pipeline as typing `/slug`, no shortcut bypass) · **`UI-MOBILE-ALERTS`** (the notification centre, filterable by severity, the second Realtime surface, FR-7.ALR.001/002 + the two self-protective banners FR-7.ALR.008/009). Cross-cutting **push-notification contract** (FR-7.VIEW.003: critical **immediate** · hard-limit **immediate + always, non-suppressible** AC-7.VIEW.003.1 · pending/stale approvals **configurable** `approval_push_frequency_minutes`/`stale_queue_push_hours` AC-7.VIEW.003.2). Governed by **#2 no mobile back-door** (every approve/act/command runs the **identical** C1 node gate + C6 pipeline; Restricted needs the same audited reveal FR-1.RST.003; the deep-management set — config/permission-matrix/conflict-resolution/Modify/fleet/agent-capability/command-authoring/memory-mutation — degrades to a *"open on a wider display"* **notice**, never a silent omission) · **#3 no false-healthy on a phone** (freshness badges + the honest Live/Reconnecting/Polling indicator FR-7.RTP.004 + the two protective banners **mandatory on mobile** — a stale "all-green"/"all caught up" is the single most dangerous false-healthy view; every error/stale reads "—"/"can't confirm", never "0"/"all clear") · **#1 nothing lost** (a mobile "disable" retains the definition; a dropped push is recoverable via the persisted notification centre FR-7.ALR.001/006). All 5 states each · verification gate **CLEAN — 0 HIGH/0 MED/2 LOW** (LOW-1 fixed: Home health-score cite FR-7.VIEW.001→**FR-7.VIEW.002** non-technical rollup; LOW-2 accepted: "pinned banner" is legitimate surface-coined chrome) · OD-149–152 resolved. **The clean PERM case — no entry node minted** (third consecutive: 10, 11, 12): mobile is a **viewport treatment**, each screen inherits its desktop counterpart's node (`PERM-action.review` / `PERM-dashboard.*` / per-command FR-9.CMD.002); the Alerts/notification centre is node-free clearance-scoped chrome. **OD-150** ships v1 as **responsive web + PWA with web-push**; a native wrapper deferred → **OOS-040**. **Phase-4 debt:** the net-new **`push_subscriptions`** device-token store (owed to C7 for FR-7.VIEW.003 delivery; RLS-scoped to user, no `client_slug`); the `conversations`/`messages` chat store (OD-135) + `task_queue.originating_user_id` reused from surfaces 04/08. **Paper-vs-proven:** mobile push *delivery* reliability (web-push background delivery of a "critical, immediate, always" alert) flagged for a **Phase-5 spike** — no Phase-3 FR rests on it (fails safe to the persisted in-app record). `surface-01b-config-audit-log.md` ✅ (2026-07-01 · the **config-change audit-log viewer** — the **14th and final** Phase-3 surface — **`UI-config-audit-log`** (named by OD-099, not minted here — like `UI-COMMANDS`), the read/review counterpart to surface-01: surface-01 *writes* config + appends the `config_audit_log` row, surface-01b *reads back* who changed which knob, from→to, when, with a compliance export. Three sections: **Config-Change Timeline** (the filterable trail — section/key/actor/date; key-prefix-scoped) · **Change Detail** (one change in full — `old_value`→`new_value` diff + actor + LIVE/BOOT/REBUILD class) · **Compliance Export** (all-or-nothing, no silent truncation, `PERM-compliance.download_records`). **KEY FINDING — a Rule-0 governance gap closed via change-control (OD-153):** `config_audit_log` was the system's **third audit sink** (alongside `event_log` FR-7.LOG.001/006 + `guardrail_log` FR-7.LOG.007) but had **no FR owner** for its governance — only a `config-edit-taxonomy` rule-4 *write* mandate + a surface-01 schema stub; an unlogged/tamperable/un-exportable config-change record is a #1/#3 violation. **Minted `FR-7.LOG.008` in C7** (config_audit_log view/retention/tamper-evidence/export, mirroring FR-7.LOG.007; C7 **34→35 FRs**; precedent OD-097→FR-7.ALR.009); AC-7.LOG.008.1–.5 (export all-or-nothing · retention floor · append-only+tamper-evident · redaction-tombstone on user-erasure · secrets-never-appear). **Clean PERM case — no node minted** (third-plus consecutive: 10/11/12/01b): view is **key-prefix-scoped by existing `PERM-config.*`** (surface-01's RLS — a caller sees only the audit history of sections they may manage; `#infra` Super-Admin-only), export gated by catalogued `PERM-compliance.download_records`. All 5 states each (a failed/empty load never reads "no changes ever" — Empty/Error distinguish "you can't see any" from "none exist"; export never a silent partial) · verification gate **CLEAN-WITH-FIXES — 0 HIGH/2 MED/3 LOW reconciled** (MED-1 OD-102 mis-cite → re-cited to the SECRET edit class in surface + AC-7.LOG.008.5; **MED-2 `config-edit-taxonomy` rule 4 amended via change-control** — audit mandate broadened LIVE → LIVE/BOOT/REBUILD, reconciling it with config-registry + surface-01; LOW-1 "11"→"10 editable"; LOW-2 accepted; LOW-3 banner re-cite) · OD-153–156 resolved. **Phase-4 debt:** `config_audit_log` owed to the C2 FR-2.MNT.017 / C10 FR-10.DEL.004 erasure walk (actor-attribution redaction-tombstone). **✅ Phase 3 COMPLETE (14 of 14 surfaces) → next is Phase 4 (Data model).** |
| 4 — Data model | 🟢 **COMPLETE — signed off 2026-07-01 (session 44).** `spec/04-data-model/`: **playbook finalized** (approach→full mechanical detail) + 5 files. **Harvest** (subagent fan-out over 14 surfaces + 11 components + config registry) → `_data-inventory.md` (~40 tables in 14 groups; authoritative 21 `DATA-*` matrix ids; **16 net-new Phase-3 stores** catalogued + owed-back; **R1** — `client_slug` DELETED not "label-only" per OD-096/FR-10.ISO.001; **7 schema ODs** OD-P4-01…07 surfaced). **`schema.md`** — one coherent schema: a `Types` section (all enums) + 14 groups (identity/auth · RBAC · memory · tools · prompt · harness · guardrails · observability · agent-design · proactive · chat · config · mgmt-plane · compliance); every table typed w/ PK/FK/constraints; all net-new stores designed (`memory_conflicts`, `consolidation_approvals`, `injection_quarantine`, `task_history`, `notifications`, `push_subscriptions`, `agent_health_metrics`, `agent_result_cache`, `execution_plans`, `commands`, `signal_weights`, `conversations`/`messages`, + fields `task_queue.originating_user_id`, `guardrail_log.escalated_at`, two-person-auth on `deletion_requests`); **NO `client_slug` on any app table** (only `client_registry`/`deployment_health`/`offboarding_records` on the separate mgmt deployment, ADR-001 §7). **`rls-policies.md`** — ADR-006 static data-driven `(select …)` initPlan (AF-067), human-path RLS vs agent-path `service_role`, per-table policy summary, the three non-negotiables in the RLS layer. **`indexes.md`** — HNSW vector (CONCURRENTLY) + `(status, created_at)` queue family + silent-failure-detector join + RBAC policy-read + net-new store indexes. **`migrations.md`** — expand-contract (`migration-discipline.md`), 0001 ordering, mgmt-plane separate lineage, worked examples (drop `agents.system_prompt`, embedding-model swap), per-deployment failure isolation, AF-065 paper-until-tested. **Verification gate (independent zero-context, checks a–f): CLEAN-WITH-FIXES — 0 HIGH · 2 MED · 4 LOW** (coverage complete/no orphans · no-`client_slug` CLEAN · all enum spot-checks match source · append-only sinks + sole-writer + hard_limit≠approved CHECK + two-person auth all present · migrations respect expand-contract/CONCURRENTLY/idempotent-seed/failure-isolation; source subagent 10/10 load-bearing claims verified). Reconciled: **MED-1** `deletion_requests` executor-distinctness CHECK added (AC-10.DEL.006.2 no-self-execution now DB-enforced); **MED-2/LOW-1** store renames recorded as **R2**; **LOW-2** doc-enum note. **Sign-off finalization DONE:** 7 OD-P4 resolutions accepted (recommended options); the **16 net-new-store owed-back `DATA-` cites applied to component FRs via change-control** (C2 memory_conflicts/consolidation_approvals · C3 idempotency_ledger · C5 task_history + task_queue.originating_user_id · C6 injection_quarantine + guardrail_log.escalated_at · C7 notifications + push_subscriptions · C8 agent_health_metrics/execution_plans/agent_result_cache · C9 commands/signal_weights/conversations+messages · C10 deletion_requests two-person-auth); the **R1 `client_slug` clerical amendment** applied to C3–C6 (C2 needed none); `traceability-matrix.csv` wired (DATA-consolidated-in-schema header note). **→ Phase 5 (Non-Functional) next.** |
| 5 — Non-functional | 🟢 **COMPLETE — signed off 2026-07-01 (session 45).** Verification gate CLEAN (0 HIGH / 3 MED / 3 LOW, all reconciled). `spec/05-non-functional/`: playbook finalized (approach→full mechanical detail) + **9 files** — `_nfr-inventory.md` (six-agent harvest ledger, ~82 candidates) + 8 domain files: `security.md` (**17 NFR-SEC** + the exemplar row-shape) · `infrastructure.md` (14 INF) · `performance.md` (12 PERF, aspirational spike-confirmed targets per RP-4) · `observability.md` (16 OBS + **2 NFR-A11Y** baseline per RP-3) · `cost.md` (10 COST) · `compliance.md` (11 CMP, incl. the audit-sink immutability trigger) · `backup-dr.md` (8 DR, RPO~1h / restore-rehearsal monthly+per-migration per RP-2) · `test-strategy.md` (the keystone — the **AF de-risking schedule**). **~90 NFR-\* across 8 domains**, each *referencing* (not re-speccing) its functional owner FR/ADR + naming the AF-\* that proves it. **Cardinal rule: reference-don't-re-spec** — the machinery already lives in C6/C7/C10/ADRs; NFRs add the posture/threshold/duty/verification. **Four risk-posture ODs (OD-157–160) operator-decided:** **OD-157 (RP-1)** the **six launch-gating spikes** (AF-068 injection · AF-069 restore · AF-001 cost · AF-067 RLS-latency · AF-078 webhook · AF-077 brute-force) that must PASS before go-live, vs *blocking-by-posture* mechanisms (isolation/audit-immutability/freeze/RLS-coverage/expand-contract/#3-watchdogs) built regardless with build-time AF proof, vs *fast-follow* accuracy-EVALs behind safe postures (shadow-retain/flag-only/human-in-loop/fails-safe); **OD-158 (RP-2)** restore rehearsal monthly + per-migration; **OD-159 (RP-3)** a11y baseline floor (full WCAG→OOS-041); **OD-160 (RP-4)** aspirational perf targets tagged "confirm-by-AF, not proven". **Gap-sweep change-controls:** +AF-138 (mobile web-push delivery, fast-follow, fails-safe) · +OOS-041 (WCAG deferral) · +`recovery_tier` + `haiku_audit_window_days` + `haiku_gate_disagree_threshold` config keys (were owed but absent — Rule-0 gaps closed). **`id-conventions.md` +`DR` domain code.** Verification gate (independent zero-context, checks a–f): 0 HIGH, no overclaim (every perf number tagged paper-not-proven, never an SLO), all three non-negotiables provably covered; 3 MED + 3 LOW reconciled (dangling `recovery_tier` cite→config key added; stale haiku-missing note→corrected; test-strategy "every AF" completeness→+AF-063/040/041/113; 2 cite-precision fixes). **→ Phase 6 (Issue decomposition) next** once signed off. |
| 6 — Issues | ⚪ **not started — GATED on a full whole-spec audit first (operator-requested).** Before slicing into issues, run the **pre-Phase-6 full-spec audit** (Phases 0–5 end-to-end: cross-phase ID resolution · traceability completeness · cross-phase consistency · change-control integrity · contradiction hunt · three-non-negotiables end-to-end). Repeatable procedure: **`spec/00-foundations/pre-phase-6-audit-playbook.md`** (may run in a fresh chat — reads CLAUDE.md → README → the playbook → executes). A clean audit (0 unresolved HIGH) clears Phase 6. |

Decisions chosen: spec home = git markdown repo · big decisions = grill load-bearing, fast-track rest · start = foundations then components in order.

ADRs: 🟢 ADR-001 (isolation) · 🟢 ADR-002 (coverage → Maturity + Retrieval Sufficiency; closes OD-008) · 🟢 ADR-003 (cost model — client-side viability + cost ladder; closes OD-003) · 🟢 ADR-004 (concurrency — per-entity serialize + optimistic validate-and-commit; closes OD-004) · 🟢 ADR-005 (deploy/provisioning — canary + release-train, scripted provisioning, bounded version skew; closes OD-005) · 🟢 ADR-006 (RLS/dynamic roles — static data-driven policies over live permission tables, intra-client only, instant grant/revoke; closes OD-006) · 🟢 ADR-007 (injection posture — containment-first, detection-as-signal, embedding scan off by default; closes OD-007) · 🟢 ADR-008 (backup/DR — hourly client-owned off-platform snapshot default + PITR opt-in upsell + operator-verified restore + backup-health on the mgmt-plane push; golden rule = source data referenced not copied; closes OD-009). **All load-bearing ADRs landed; no Phase-0 blockers remain.** Spikes: 🟡 **AF-003 (vendor-claims) DOCS pass done** — 3 claims stale/refuted (AF-010 Gmail quota, AF-011 GHL limits, AF-014 GHL refresh-token), 1 design fork (AF-012 Slack history throttle → OD-011); AF-013/015/016/018/020/021 verified; AF-019 stays SPIKE/LOAD-open. The 3 SPIKE/EVAL priority spikes (AF-001 cost, AF-002 retrieval, AF-004 provisioning) need a runnable prototype — deferred until build. New ODs: OD-011 (Slack app class, 🟡, Phase-1 Slack connector). New AF: AF-068 (injection containment red-team), AF-069–072 (backup/DR — restore-works, mgmt-API fields, region/residency, dump-at-scale).

**Phase 1 entered (2026-06-24).** Component 0 (Login) scope finalized as the golden exemplar (auth-only: login/2FA/sessions/invites/seed/recovery/webhook-auth; connector OAuth → C3, roles/RLS → C1 — see phase-playbooks "Component 0 entry finalization"). **Supabase Auth research-first gate run** (Block J in feasibility-register, 2026-06-24): a dated primary-source pass **refuted/corrected 6 design-doc claims** — 7-day refresh-token TTL (refresh tokens never expire, rotate single-use), HTTP-only cookies (not the default), "server-side session continues mid-task" (no such object), org-wide `two_factor_required` config (must be built via aal2 RLS + app gating), 72h invite links (24h hard cap), Microsoft Authenticator (unnamed; rests on RFC-6238). New **AF-073–077**; **AF-067 sharpened** (the `(select …)` initPlan rule — `STABLE` alone ≠ once-per-statement). **Component-0 FRs drafted + resolved (2026-06-24, Session 16):** `spec/01-requirements/component-00-login.md` — **42 live FRs** (AUTH ×10 · SESS ×8 · INV ×7 · SEED ×3 · REC ×6 · WHK ×8) + 1 retired (REC.004), all at `Ready`, citing Block J for vendor facts; the 6 refuted design-doc claims carried as a doc-reconciliation table. **OD-012…OD-023 all resolved** — headline: **OAuth-only for client-tenant users; password+2FA for external Super Admins only** (OD-018), which dissolved the credential-reset recovery flow (REC.004 retired) and the phone field. **Verification gate run** (2 zero-context subagents): orphan/contradiction pass **clean**; quality pass found **6 findings, all reconciled** (seed-race → atomic guard FR-0.SEED.003; +FR-0.AUTH.010 audit-completeness, +FR-0.INV.007 email-bounce, +FR-0.REC.007 stale-request; missed-webhook seam parked to C2/C3/C7; backup covered by ADR-008). New **AF-078** (webhook verification, block K). Glossary +AAL/aal2, +refresh-token rotation, +JWKS. **Component 0 is signed off** (user-authorized, delegated) — 42 FRs `Approved`, `system-map/00-login.md` built, `traceability-matrix.csv` wired, OOS-015 logged (email-bounce deferral), ADR-007 cross-ref reconciled. Next: component 1 (RBAC).

**Component 1 (RBAC) drafted + resolved + Approved (2026-06-24).** `spec/01-requirements/component-01-rbac.md` — **37 FRs** (ROLE ×5 · PERM ×7 · CLR ×6 · RST ×3 · RLS ×8 · USR ×5 · AUD ×3), all `Approved`. ADR-006 is the spine (permissions-in-data · static data-driven RLS via `(select …)` initPlan · instant grant+revoke · intra-client only · harness/RLS division · human-path-RLS vs agent-path-service_role). **OD-024…OD-031 resolved** (delegated C0-style). **Wrote the owed `standards/rbac.md`** (12 binding rules, promised since ADR-006). **Verification gate clean:** orphan/contradiction pass CLEAN (4 traps avoided: no `client_slug` in policies, no RLS-guards-agent assumption, Restricted never a role-default, no role-name-in-policy); quality pass found **5 findings clustered at the service-role/mid-task seam, all reconciled** → +**FR-1.RLS.007** (a service_role task halts+quarantines on mid-task deactivation/clearance-revoke before a consequential side effect; benign session-expiry continues, reconciling C0 FR-0.SESS.006), +**FR-1.RLS.008** (RLS/harness divergence observable), +**OD-031**, +**AF-081** (agent-path audit completeness), reactivation re-grant branch. New **AF-079/080/081** (block L). Caught + resolved a real design contradiction (L438 vs L452 — Restricted is per-individual only). Homed the C0 PERM stubs + role tables; `system-map/01-rbac.md` built; matrix wired (37 rows).

**Component 2 (Memory) drafted + resolved + Approved (2026-06-25).** `spec/01-requirements/component-02-memory.md` — **57 FRs** (MEM ×2 · ENT ×5 · TAG ×3 · ING ×10 · WRT ×7 · RET ×7 · MNT ×17 · VEC ×3 · MAT ×3), **56 Approved + 1 v2-deferred** (cold storage). The heart of the system — the durable, entity-organised, sensitivity-tagged business brain. Three ADRs converge: **ADR-002** (Maturity drives cold-start gating; Retrieval Sufficiency drives the `[Building]` flag), **ADR-003** (≤1 Sonnet writer wrapped in cheap Haiku gates; the design's two ingestion filters reconcile to ADR-003's selective-writing + sensitivity-classify gates — no third model layer), **ADR-004** (sole-writer `service_role` + per-entity validate-and-commit). C2 **consumes** C1's clearance/visibility/Restricted model (enforced **before** ranking, FR-2.RET.004) and **owns the mechanisms C1 only ruled on** — sensitivity+entity-type tagging, the retrieval pipeline, never-auto-inject-Restricted. **OD-032…OD-038 resolved** (5 delegated C0/C1-style; **OD-034 cold-storage deferred to v2 → OOS-016** and **OD-038 compliance-erasure rule homed here, backups seamed to Phase 5** decided by the user directly). **Verification gate clean:** orphan/contradiction pass **CLEAN** (all design intents L1338–1967 mapped, 3 deferrals logged OOS-016/003/017, all 5 traps PASS, no ADR/C1 contradictions; one citation slip L1407→**L1414** + two cross-ref slips fixed); quality pass found **7 findings, all reconciled** → +**FR-2.WRT.007** (embedding-failure halts commit, never stores a null/invalid embedding), +**AC-2.WRT.006.3** (mid-task revocation re-check at commit, realizing C1 FR-1.RLS.007), ingestion-queue durability + escalation (FR-2.ING.003/MNT.010, closed a Rule-0 dangling decision), transitive **compliance erasure** (FR-2.MNT.017 walks supersede chain + merged/summarised derived rows), escalation ACs on the human-gated queues, re-embed completeness gate (AC-2.VEC.003.2). New **AF-082** (entity-resolution accuracy — the fragmentation risk). `system-map/02-memory.md` reconciled; matrix wired (57 rows). **Next: component 3 (Tool layer)** — the connectors behind C2's three ingestion pipelines + the live-data fetch for relevance cross-check; resolves **OD-011** (Slack app class) and propagates the AF-003 corrected vendor limits.
