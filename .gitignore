.DS_Store
.claude/*
!.claude/settings.json
*.log

# build-phase (TypeScript / Node — ADR-009)
node_modules/
node_modules   # also match node_modules SYMLINKS (offline-test setup symlinks a sibling's node_modules; trailing-slash rule above only matches dirs)
.env
.env.local
dist/
*.tsbuildinfo

# spike dry-run artifacts (mock tokens — not evidence)
spikes/*/results/*.dryrun.*

# Supabase CLI local cache (linked-project, temp)
supabase/
